# .env.example - Template (English)
# Copy this file to .env and fill in the values. Do NOT commit real credentials.

# PostgreSQL
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# Notes:
# - Keep .env out of version control. The repository's .gitignore already ignores .env.
# - Use strong, unique passwords for POSTGRES_PASSWORD.
